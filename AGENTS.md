# Repository Agent Rules

- After completing and verifying a self-contained feature, automatically commit all changes that belong to that feature unless the user explicitly asks not to commit.
- Before committing, review `git status`, keep unrelated work out of the commit, and report the verification performed.
